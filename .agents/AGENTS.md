# Project Specific Rules

- **Database**: This project explicitly uses **MySQL**. Disregard any documentation (e.g., the PDF project task) that dictates the use of PostgreSQL. Do not attempt to migrate the database to PostgreSQL.
- **Frontend Stack**: This project explicitly uses **Laravel Blade** for the web interface. Disregard any requirements for a separate SPA/API architecture unless explicitly requested by the user.
